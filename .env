# Deterministic AI UI Builder - environment variables
# Copy these keys into Vercel Project Settings -> Environment Variables.

# Required for live LLM-backed planning/explaining.
OPENAI_API_KEY=your_openai_api_key_here

# Optional model override (defaults to gpt-4.1-mini if omitted).
OPENAI_MODEL=gpt-4.1-mini
